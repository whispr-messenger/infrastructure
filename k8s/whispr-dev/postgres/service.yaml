apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: whispr-dev
  labels:
    app: postgres
    tier: infrastructure
    environment: development
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
      protocol: TCP
