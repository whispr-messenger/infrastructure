apiVersion: v1
kind: Service
metadata:
  name: media-service
  namespace: whispr-prod
  labels:
    app: media-service
    tier: backend
    environment: production
  annotations:
    description: "Service for Media microservice"

spec:
  type: ClusterIP

  selector:
    app: media-service # --> Service route toward pods with label

  ports:

  - name: http # --> Name port (required by Istio --> protocol detection)
    port: 3012
    targetPort: http
    protocol: TCP

  - name: grpc # --> gRPC port
    port: 50012
    targetPort: grpc
    protocol: TCP

  sessionAffinity: None