apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: whispr-prod
  labels:
    app: user-service
    tier: backend
    environment: production
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
  - name: http
    port: 3011
    targetPort: 3011
    protocol: TCP
  - name: grpc
    port: 50011
    targetPort: 50011
    protocol: TCP
  sessionAffinity: None