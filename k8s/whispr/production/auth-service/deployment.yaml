apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: whispr-prod
  labels:
    app: auth-service
    tier: backend
    environment: production
  annotations:
    description: "Authentication microservice for Whispr Messenger"
    maintainer: "gabriel.lopez@epitech.eu"
    argocd.argoproj.io/sync-wave: "2"
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    # Pod template specification
    spec:
      serviceAccountName: auth-service-sa
      # Security context for the pod
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: whispr-auth-api
          # The image is automatically updated by the CD pipeline with yq
          image: ghcr.io/whispr-messenger/auth-service/auth-service:sha-2dc6c70 # Placeholder, replaced by CD pipeline
          ports:
            - name: http
              containerPort: 3001
              protocol: TCP
            - name: grpc
              containerPort: 50051
              protocol: TCP
          # Environment variables from ConfigMap
          env:
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: auth-service-db-secret
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-service-db-secret
                  key: password
            - name: DB_URL
              value: "postgresql://$(DB_USERNAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)"
            - name: DATABASE_URL
              value: "postgresql://$(DB_USERNAME):$(DB_PASSWORD)@$(DB_HOST):$(DB_PORT)/$(DB_NAME)"
            - name: REDIS_USERNAME
              valueFrom:
                secretKeyRef:
                  name: auth-service-redis-secret
                  key: username
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auth-service-redis-secret
                  key: password
            - name: JWT_PRIVATE_KEY_FILE
              value: /app/secrets/jwt/jwt_private_key.pem
            - name: JWT_PUBLIC_KEY_FILE
              value: /app/secrets/jwt/jwt_public_key.pem
          envFrom:
            - configMapRef:
                name: auth-service-config
            - secretRef:
                name: auth-service-db-secret
            - secretRef:
                name: auth-service-redis-secret
            - secretRef:
                name: auth-service-redis-sentinel-secret
            - secretRef:
                name: auth-service-twilio-secret
          # Resource limits and requests
          resources:
            requests:
              memory: "128Mi"
              cpu: "10m"
            limits:
              memory: "512Mi"
              cpu: "200m"
          # Security context for the container
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            capabilities:
              drop:
                - ALL
          # Liveness probe - checks if the container is alive
          livenessProbe:
            httpGet:
              path: /auth/v1/health/live
              port: http
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
            successThreshold: 1
          # Readiness probe - checks if the container is ready to serve traffic
          readinessProbe:
            httpGet:
              path: /auth/v1/health/ready
              port: http
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
            successThreshold: 1
          # Startup probe - gives more time for initial startup
          startupProbe:
            httpGet:
              path: /auth/v1/health/live
              port: http
            initialDelaySeconds: 15
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 24 # 135 seconds total
            successThreshold: 1
          # Volume mounts for writable paths (distroless needs specific writable dirs)
          volumeMounts:
            - name: tmp
              mountPath: /tmp
            - name: logs
              mountPath: /app/logs
            - name: jwt-private
              mountPath: /app/secrets/jwt/jwt_private_key.pem
              subPath: jwt_private_key.pem
              readOnly: true
            - name: jwt-public
              mountPath: /app/secrets/jwt/jwt_public_key.pem
              subPath: jwt_public_key.pem
              readOnly: true
      # Volumes
      volumes:
        - name: tmp
          emptyDir: {}
        - name: logs
          emptyDir: {}
        - name: jwt-private
          secret:
            secretName: auth-service-jwt-private-secret
            items:
              - key: JWT_PRIVATE_KEY
                path: jwt_private_key.pem
        - name: jwt-public
          secret:
            secretName: auth-service-public-jwt-secret
            items:
              - key: JWT_PUBLIC_KEY
                path: jwt_public_key.pem
      # Restart policy
      restartPolicy: Always
      # DNS policy
      dnsPolicy: ClusterFirst
      # Termination grace period
      terminationGracePeriodSeconds: 30
