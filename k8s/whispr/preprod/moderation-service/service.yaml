apiVersion: v1
kind: Service
metadata:
  name: moderation-service
  namespace: whispr-preprod
  labels:
    app: moderation-service
spec:
  type: ClusterIP
  selector:
    app: moderation-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
