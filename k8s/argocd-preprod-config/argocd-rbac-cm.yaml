---
# RBAC configuration for ArgoCD — preprod
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd-preprod
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
data:
  policy.default: role:readonly
  policy.matchMode: glob
  scopes: '[groups]'

  policy.csv: |
    # Admin access for whispr-messenger org members in whispr-team
    g, whispr-messenger:whispr-team, role:admin
    g, whispr-messenger, role:developer

    # API admin account (deployer bot — sync + manage apps)
    g, api-admin, role:admin

    # Custom developer role with specific permissions
    p, role:developer, applications, *, */*, allow
    p, role:developer, applicationsets, *, */*, allow
    p, role:developer, clusters, get, *, allow
    p, role:developer, repositories, *, *, allow
    p, role:developer, projects, *, *, allow
    p, role:developer, logs, get, */*, allow
    p, role:developer, exec, create, */*, allow

    # Read-only access for all authenticated users
    p, role:readonly, applications, get, */*, allow
    p, role:readonly, applicationsets, get, */*, allow
    p, role:readonly, clusters, get, *, allow
    p, role:readonly, repositories, get, *, allow
    p, role:readonly, projects, get, *, allow
    p, role:readonly, logs, get, */*, allow
