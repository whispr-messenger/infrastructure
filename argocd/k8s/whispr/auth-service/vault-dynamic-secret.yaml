---
# VaultDynamicSecret fetches dynamic database credentials from Vault
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultDynamicSecret
metadata:
  name: auth-service-db-creds
  namespace: whispr-prod
spec:
  vaultAuthRef: auth-service-vault-auth
  mount: database
  path: creds/role-auth-service
  destination:
    create: true
    name: auth-service-db-secret
  rolloutRestartTargets:
    - kind: Deployment
      name: auth-service
  renewalPercent: 67
  revoke: true
