---
# VaultDynamicSecret fetches dynamic database credentials from Vault
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultDynamicSecret
metadata:
  name: notification-service-db-creds
  namespace: whispr-prod
spec:
  vaultAuthRef: notification-service-vault-auth
  mount: database
  path: creds/role_notification_service
  destination:
    create: true
    name: notification-service-db-secret
  rolloutRestartTargets:
    - kind: Deployment
      name: notification-service
  renewalPercent: 67
  revoke: true
